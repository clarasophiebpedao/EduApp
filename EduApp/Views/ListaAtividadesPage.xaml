<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EduApp.Views.ListaAtividadesPage"
             Title="Minhas Atividades">

    <Grid Padding="15">
        <CollectionView ItemsSource="{Binding Atividades}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="LightGray" CornerRadius="8" Padding="15" HasShadow="True">

                        <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto">

                            <Label Text="{Binding Titulo}" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   Grid.Row="0" Grid.Column="0"/>

                            <Label Text="{Binding Pontos, StringFormat='{0} pts'}" 
                                   TextColor="Green" 
                                   FontAttributes="Bold" 
                                   Grid.Row="0" Grid.Column="1"/>

                            <Label Text="{Binding Descricao}" 
                                   TextColor="Gray" 
                                   Margin="0,5,0,0"
                                   Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"/>

                            <Button Text="Entregar Tarefa"
                                   Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EntregarCommand}"
                                   CommandParameter="{Binding .}"
                                   BackgroundColor="Purple"
                                   TextColor="White"
                                   Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                   Margin="0,10,0,0"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>